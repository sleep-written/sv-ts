# SV-TS
Requires node v24+

-   Install dependencies:
    ```shell
    npm i
    ```

-   Transpile the project:
    ```shell
    node --run build
    ```

-   Copy the `./dist/index.js` file into Synthesizer V 2 Scripts folder.